[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "egydata"
version = "0.1.0"
description = "Egypt data toolkit"
authors = [{name="Your Name"}]
requires-python = ">=3.8"
